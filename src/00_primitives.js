// @flow


const amount: number = 123.5;

const name: string = "John";

const isItTuesday: boolean = true;

const price: Object = { amount: 123, currency: 'USD' };

const printName = (person: { name: string }): void => console.log(person.name);
